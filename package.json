{
  "name": "circ-hq-playbook",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "verify": "node scripts/verify-public.mjs",
    "test:rules": "firebase emulators:exec --project demo-circ-hq-rules --only firestore \"node --test tests/firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.18.0",
    "firebase-tools": "15.28.2"
  }
}
